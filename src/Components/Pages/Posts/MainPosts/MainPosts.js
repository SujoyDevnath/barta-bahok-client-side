import { Box, Typography } from '@mui/material';
import React from 'react';

const MainPosts = () => {
    return (
        <Box sx={{ width: "55%" }}>
            <Typography variant="h4" sx={{ fontWeight: 'bold' }}>This is posts route</Typography>
        </Box>
    );
};

export default MainPosts;