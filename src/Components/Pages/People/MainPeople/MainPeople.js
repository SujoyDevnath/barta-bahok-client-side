import { Typography } from '@mui/material';
import { Box } from '@mui/system';
import React from 'react';

const MainPeople = () => {
    return (
        <Box sx={{width: "55%"}}>
            <Typography variant="h4" sx={{fontWeight: 'bold'}}>This is people route</Typography>
        </Box>
    );
};

export default MainPeople;